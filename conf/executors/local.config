/*
 * -------------------------------------------------
 *  Nextflow config for running on Local
 * -------------------------------------------------
 * Defines bundled configurations for process running
 * on Local
 */
process {
    executor = 'local'
}
