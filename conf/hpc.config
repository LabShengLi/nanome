/*
 * -------------------------------------------------
 *  Nextflow config file for JAX HPC
 * -------------------------------------------------
 * Defines bundled specific input params and everything required
 * to run on HPC.
 */

params {
	processors = 8
}
